# Tutorials

[![GitHub](https://img.shields.io/badge/GitHub-181717?style=flat&logo=github&logoColor=white)](https://github.com/kurtvalcorza/smoldocling-document-extraction-pipeline)
[![Open In Colab](https://colab.research.google.com/assets/colab-badge.svg)](https://colab.research.google.com/github/kurtvalcorza/smoldocling-document-extraction-pipeline/blob/main/tutorials/smoldocling_document_extraction_colab.ipynb)
[![Hugging Face](https://img.shields.io/badge/%F0%9F%A4%97%20Hugging%20Face-docling--project%2FSmolDocling--256M--preview-ffcc4d?style=flat)](https://huggingface.co/docling-project/SmolDocling-256M-preview)
[![Upstream](https://img.shields.io/badge/Upstream-docling--project%2Fdocling-181717?style=flat&logo=github&logoColor=white)](https://github.com/docling-project/docling)
[![arXiv](https://img.shields.io/badge/arXiv-2503.11576-b31b1b.svg)](https://arxiv.org/abs/2503.11576)

Notebook specification: **DIMER Notebook Specification 2.0** — the notebook is **standalone** (§4) and declares its profile and pedagogical mode (§3.4): it is
generated by `tools/build_notebook.py` from `tools/notebook_template.py` and carries the repository's three
pipeline modules (`pipeline.py`, `metrics.py`, `samples.py`), the model identity, the snapshot manifest and the
runtime pins, so the exported `.ipynb` keeps working without this repository being reachable. Do not edit the
notebook by hand; edit the package or the template and regenerate (`python tools/build_notebook.py`; `--check` is
enforced by the validator and CI).

| Notebook | Profile | Mode | Carrier | Capability | Default runtime | Sample | BYOD | Run-all | Release status |
|---|---|---|---|---|---|---|---|---|---|
| `smoldocling_document_extraction_colab.ipynb` | `E2E` | `GUIDED` | standalone (generated) | document page image → DocTags markup and bounded supervised fine-tuning of one instruction (through the DocTags output) with `docling-project/SmolDocling-256M-preview` (Apache-2.0; SigLIP tiles → 64 visual tokens per 512-px tile → 32-layer SmolLM2 decoder, greedy decoding) — the safetensors snapshot digest-verified against the inline 13-file manifest before loading; the inference contract on a synthetic 512×512 drawing for two prompts (a one-sentence caption and a counting question, generated text with no score: combined input manifest, over-long-prompt probe, structural checks, `evaluation_report` always `not-measurable`); a digest-pinned real line set (800 MIT-licensed Belfort handwritten lines with crowdsourced transcripts, eight parquet row groups pinned by SHA-256 and byte total and read over HTTPS range requests at an immutable Hub revision) split by line without leakage; micro and macro CER / WER and exact match (in the carried `metrics.py`) beside the empty-string and constant-transcript baselines; bounded fine-tuning of the last eight SmolLM2 decoder layers and the final norm with the causal language-model loss on cached prefix hidden states and validation-CER epoch selection; held-out evaluation with a four-way comparison and the hypothesis lengths; six held-out lines and the report page re-converted after adaptation; safetensors adapter export with verified transcript parity | CUDA when present (CPU is not practical for the default path); float32 | 600 / 60 / 140 lines read from the Hugging Face Hub at run time (~44 MB of a 210 MB shard, MIT) | one zip or directory of line images plus a `transcripts.csv` (`file`, `text`, optional `id`; gated off by default) | ~25 min of cell time on a Tesla T4 (frozen scoring of 140 lines 157 s, the prefix cache 337 s, eight epochs with validation scoring ≈ 18 min) | **Release-grade** — clean-runtime Kaggle Tesla T4 execution of `bc77841` / `aaf8fbf0` recorded 2026-09-21 in `../docs/release-verification.md` |

## What changed from the inference-only tutorial

The previous notebook (`TASK-INFERENCE`) converted the synthetic report page and reported `sample-sanity` with a word error rate and element counts against words rendered in code. This notebook keeps that page and its inference contract as Section 5 and adds the adaptation contract for one instruction (`Convert this page to docling.`) around it: a real labelled line set fetched with per-row-group pins, a corpus-level protocol in the carried `metrics.py` (uncapped micro and macro CER / WER, exact match, hypothesis length) with two non-adapted baselines, a bounded fine-tuning with the model's own objective on cached prefix hidden states, line panels, the report page re-converted after the shared decoder was tuned, and an artifact that reloads. The per-image `evaluation_report` helper is unchanged; `pipe.evaluate` is the corpus-level stage (`measured` / `measured-small-sample`). The pipeline now loads in float32 on every device (the earlier notebook used bfloat16 on CUDA), batches one instruction over many images with left padding (`transcribe`, which also strips the DocTags to the text they carry), and admits text lines wider than 4,096 px (sides within 16..16,384 px, at most 4096² pixels).

## Conformance notes

- **Standalone carrier (ST1–ST8):** the primary path performs no clone, repository install, or repository import. Section 2 embeds the repository's three modules verbatim in topological dependency order (each tagged `metadata.dimer.embedded_module`; the only rewrites make `DEFAULT_WEIGHTS_DIR` working-directory-relative and remove internal relative package imports), Section 3 carries the 13-file snapshot manifest inline and asserts it against the module's constants before fetching, and `NOTEBOOK_SOURCE` records the repository, revision and module digests in the exports.
- **Parity (PAR1–PAR4):** `tests/test_notebook_parity.py` and `tools/validate_release_assets.py` fail when the carried modules, the inline manifest, or the inline pins differ from the repository, or when the notebook is not byte-identical to the generator's output.
- **Model acquisition (MOD1–MOD9, MOD12):** `stage_missing_files(WEIGHTS_DIR, allow_download=True)` fetches only the manifest entries a fresh runtime lacks, at the pinned revision (never `main`); `verify_snapshot` re-hashes every entry and asserts the pinned `model.safetensors` digest and byte count before the processor or the model is constructed; `SmolDoclingPipeline.from_pretrained(weights_dir=WEIGHTS_DIR)` loads the verified files through the native classes (`local_files_only=True`, `trust_remote_code=False`) in float32 and picks CUDA when it is visible. No cell outside the carried modules calls `huggingface_hub`, `transformers`, `safetensors`, `pyarrow`, `urllib`, `torch.optim` or the model's processor directly.
- **Data (DAT1–DAT9, ST6):** the default sample is the Belfort-line dataset (`Teklia/Belfort-line` at the immutable parquet-conversion revision `c4a74bbd39f2df314752e7e6026649a39d365cbb`, MIT): the first eight row groups of `default/test/0000.parquet` read with HTTPS range requests in the carried `samples.py` (the shard's declared size and row count checked first, each row group refused unless its SHA-256 and byte total match `ROW_GROUP_PINS`; about 44 MB), and `read_corpus` makes each row a record with its 128-px line image and its transcript (whitespace runs collapsed); `build_sample_dataset(seed=SPLIT_SEED)` draws 600 / 60 / 140 and `check_split_disjoint` asserts no image (by decoded-pixel digest) is shared. The template adds a second *External access* bullet naming the host, the revision, the byte count and the licence.
- **Stages (§4.1):** `fetch_corpus` + `read_corpus` + `build_sample_dataset` / `load_byod_dataset` + `split_dataset` (sample acquisition) → `validate_dataset` per split + `check_split_disjoint` + refusal probes (validation, with three digests) → `validate_inputs` per prompt + the over-long-prompt refusal probe + `generate` for two prompts with structural checks + `evaluation_report` on the drawing (inference contract) → `empty_baseline` + `constant_baseline` + `pipe.evaluate` on the frozen `Convert this page to docling.` instruction with four hypotheses printed (baselines) → `pipe.adapt` (bounded fine-tuning) → `pipe.evaluate` on validation and test with the comparison (evaluation) → the line panels + the report page again (inference) → `pipe.save_artifact` → `SmolDoclingPipeline.from_artifact` with the parity assertion (export and reload).
- **Score semantics (UNC1–UNC4):** DocTags are generated markup with no score, and the notebook says so before the first call; the corpus rates are uncapped Levenshtein rates over one crowdsourced transcription at stated normalisations, with the hypothesis length beside them so a rate above 1.0 is read as over-generation; no acceptance threshold ships. The page re-converted after adaptation carries a `sample-sanity` verdict against words and counts rendered in code, and the notebook names the missing dispersion.
- **Baselines and evidence (EVAL6/EVAL10/EVAL11):** the empty string, the constant transcript and the frozen model are scored on the same 140 lines as the adapted model, on the same rates; every metric is labelled as one seeded split of one sample with no dispersion estimate, and the notebook says explicitly where the frozen model sits against the empty baseline (build record: the frozen model answers the transcription instruction with almost nothing — 62 of the 140 hypotheses are empty and 100 are three characters or fewer — while 21 lines loop on a digit or a syllable to the 160-token budget (29 truncated), which is how a CER above 1.0 coexists with hypotheses shorter than the references) and that the gain is measured on one French council's cursive only.
- **Split integrity (SPL1–SPL8):** the split is seeded and image-disjoint; validation (60 lines) selects the epoch on CER and the test split is used for nothing but the final evaluation; the notebook states that lines cut from the same page share a hand and must be split by page, writer or volume.
- **Ceilings (VAL6):** `MIN_IMAGE_SIDE` (16), `MAX_IMAGE_SIDE` (16384), `MAX_IMAGE_PIXELS` (4096²), `MAX_NEW_TOKENS` (8192), `DEFAULT_MAX_NEW_TOKENS` (2048), `DEFAULT_LINE_MAX_NEW_TOKENS` (160), `DECODING` (`greedy`), the seven `INSTRUCTIONS`, `LINE_DOCTAGS`, the record bounds (8..5,000), the transcript bound (512) and the id pattern are stated in the Prerequisites and printed before the model runs; four dataset refusals and one unsupported-instruction refusal are demonstrated.
- **BYOD (DAT10–DAT19):** `USE_BYOD` defaults to `False` so the sample path never opens an upload dialog; when enabled, one zip is read member by member (names flattened, no `extractall`) or a directory walked, each row of `transcripts.csv` becomes a record, and the records flow through the same validation, image-disjoint split, baselines, fine-tuning, evaluation, export and reload cells. The expected layout and the privacy guidance are stated in the Prerequisites and in Section 4, and the training split's summary is written to `outputs/smoldocling_document_extraction_train.csv`.
- Licence: the weights are CDLA-Permissive-2.0 and the notebook, README, card and `docs/WEIGHTS.md` say so; Belfort-line is MIT and is not redistributed — the export records the corpus name, repository, revision, file, licence, language and row groups under `corpus`.
- `tools/validate_release_assets.py` performs source validation only. It does not satisfy the clean-runtime execution requirement; a release review must confirm that a recorded clean run in `../docs/release-verification.md` matches the notebook revision under review before the status is promoted to `Release-grade`.

## Outputs

The default notebook path writes (under `outputs/`):

- `smoldocling_document_extraction_train.csv` — the training split's summary table (id, file name, image size, character and word counts, transcript, provenance)
- `smoldocling_document_extraction_example_line.png` — the first training line
- `smoldocling_document_extraction_input_manifest.json` — the combined inference-contract manifest (schema, the page request, one recorded rejection finding)
- `smoldocling_document_extraction_page_frozen.json`, `smoldocling_document_extraction_page_adapted.json` — the report page's conversion, DocTags summary, timings, structural checks and `evaluation_report` before and after adaptation
- `smoldocling_document_extraction_evaluation_report.json` — baselines, frozen and adapted transcription metrics on validation and test, the per-line rows with both hypotheses, the comparison, the adaptation history
- `smoldocling_document_extraction_examples/` — six held-out lines as panels (the line image with the reference, the frozen transcript and the adapted transcript)
- `smoldocling_document_extraction_adapter/` — `adapter.safetensors` + `manifest.json`
- `smoldocling_document_extraction_result.json` — `NOTEBOOK_SOURCE`, model identity, snapshot, corpus, the report page before and after adaptation with its reports, comparison, artifact digest, reload parity, runtime

The earlier record for the superseded `TASK-INFERENCE` carrier is kept in `../docs/release-verification.md`.

## AI Assistance Disclosure

This repository’s code and accompanying documentation were developed with generative AI assistance for code development and technical writing under maintainer direction. The maintainer remains responsible for reviewing the implementation, validating results, and making release decisions. AI assistance does not constitute independent verification, provider endorsement, or release approval.
